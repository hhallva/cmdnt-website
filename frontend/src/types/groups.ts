export interface GroupDto {
    id: number;
    name: string;
    course: number;
}

export interface PostGroupDto {
    name: string;
    course: number;
}

export interface UpdateGroupDto {
    name: string;
    course: number;
}